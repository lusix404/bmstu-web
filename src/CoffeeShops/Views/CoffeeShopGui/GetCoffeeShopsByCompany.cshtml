@* @model CoffeeShops.ViewModels.CoffeeShopsListViewModel

@{
    ViewData["Title"] = "Кофейни сети";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/bg_color_style.css" />
    <link rel="stylesheet" href="~/css/header_container_style.css" />
    <link rel="stylesheet" href="~/css/logo_style.css" />
    <link rel="stylesheet" href="~/css/logout_button.css" />
    <style>
        .company-list {
            width: 70%;
            margin: 20px auto;
            color: #EDE7DD;
            font-size: 14px;
        }

        .company-item {
            display: flex;
            align-items: center;
            padding: 15px;
            border-bottom: 1px solid #EDE7DD;
        }

        .company-info {
            display: flex;
            width: 100%;
        }

        .user-login,
        .user-bdate,
        .company-actions {
            padding: 8px 10px;
            word-break: break-word;
            overflow-wrap: break-word;
            white-space: normal;
            line-height: 1.4;
        }

        .user-login {
            width: 50%;
            font-weight: bold;
        }

        .user-bdate {
            width: 20%;
        }

        .company-actions {
            width: 30%;
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }

        .company-header {
            color: #EDE7DD;
            border: 1px solid #EDE7DD;
            border-radius: 4px;
            padding: 10px 0;
            margin-bottom: 10px;
        }

            .company-header .company-info {
                display: flex;
            }

            .company-header .user-login,
            .company-header .user-bdate {
                position: relative;
                padding: 0 10px;
                border-right: 1px solid #EDE7DD;
                font-weight: bold;
            }

            .company-header .company-actions {
                padding: 0 10px;
            }

        .action-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #5E463D;
            font-size: 14px;
            background-color: #EDE7DD;
            white-space: nowrap;
        }

        h2 {
            color: #EDE7DD;
            margin-bottom: 20px;
        }

        p {
            color: #EDE7DD;
        }

        .upper-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #5E463D;
            font-size: 18px;
            background-color: #EDE7DD;
        }

        .back-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #EDE7DD;
            font-size: 18px;
            background-color: #2c1c1a;
        }

        /* Новые стили для кнопки добавления */
        .header-with-button {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .add-coffeeshop-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #EDE7DD;
            font-size: 16px;
            background-color: #2c1c1a;
        }
    </style>
</head>
<body>
    <div class="header-container">
        <div class="button-group">
            <a href="@Url.Action("Logout", "User")" class="logout-btn">Выйти</a>
            <a href="@Url.Action("GetAllCompanies", "Company")" class="back-btn">Назад</a>
        </div>
    </div>
    <div class="company-list">
        <div class="header-with-button">
            <h2>Список кофеен:</h2>
            @if (Model.Id_role == 2 || Model.Id_role == 3)
            {
                <a href="@Url.Action("AddCoffeeShopByCompany", "CoffeeShop", new { id_company = Model.Id_company })"
                   class="add-coffeeshop-btn">Добавить новую кофейню</a>
            }
        </div>

        @if (Model.CoffeeShops.Any())
        {
            <div class="company-header">
                <div class="company-info">
                    <div class="user-login">Адрес</div>
                    <div class="user-bdate">Часы работы</div>
                    <div class="company-actions">Доступные действия</div>
                </div>
            </div>

            @foreach (var user in Model.CoffeeShops)
            {
                <div class="company-item">
                    <div class="company-info">
                        <div class="user-login">@user.Address</div>
                        <div class="user-bdate">@user.WorkingHours</div>
                        <div class="company-actions">
                            <a href="@Url.Action("AddToFavsCoffeeShop", "FavCoffeeShops", new { id_cs = user.Id_coffeeshop })"
                               class="action-btn shops-btn">Добавить в избранные</a>
                            @if (Model.Id_role == 2 || Model.Id_role == 3)
                            {
                                <a href="@Url.Action("DeleteCoffeeShop", "CoffeeShop", new { id_cs = user.Id_coffeeshop, id_comp = user.Id_company })"
                                   class="action-btn shops-btn">Удалить</a>
                            }
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <p>У сети нет открытых кофеен</p>
        }
    </div>
</body>
</html> *@

@model CoffeeShops.ViewModels.CoffeeShopsListViewModel

@{
    ViewData["Title"] = "Кофейни сети";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/bg_color_style.css" />
    <link rel="stylesheet" href="~/css/header_container_style.css" />
    <link rel="stylesheet" href="~/css/logo_style.css" />
    <link rel="stylesheet" href="~/css/logout_button.css" />
    <style>
        .coffeeshops-container {
            width: 70%;
            margin: 20px auto;
            color: #EDE7DD;
            font-size: 14px;
        }

        .coffeeshops-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

            .coffeeshops-table th,
            .coffeeshops-table td {
                padding: 12px 15px;
                border-bottom: 1px solid #EDE7DD;
                vertical-align: middle;
            }

            .coffeeshops-table th {
                font-weight: bold;
                background-color: rgba(94, 70, 61, 0.3);
                border: 1px solid #EDE7DD;
            }

        .address-column {
            width: 50%;
        }

        .hours-column {
            width: 20%;
        }

        .actions-column {
            width: 30%;
        }

        .action-buttons {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }

        .action-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #5E463D;
            font-size: 14px;
            background-color: #EDE7DD;
            white-space: nowrap;
        }

        h2 {
            color: #EDE7DD;
            margin-bottom: 20px;
        }

        .empty-message {
            color: #EDE7DD;
            font-style: italic;
            padding: 20px 0;
        }

        .button-group {
            display: flex;
            gap: 10px;
        }

        .back-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #EDE7DD;
            font-size: 18px;
            background-color: #2c1c1a;
        }

        .upper-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #5E463D;
            font-size: 18px;
            background-color: #EDE7DD;
        }

        .fav-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #EDE7DD;
            font-size: 18px;
            background-color: #b567b2;
        }

        .header-with-button {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .add-coffeeshop-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #EDE7DD;
            font-size: 16px;
            background-color: #2c1c1a;
        }

    </style>
</head>
<body>
    <div class="header-container">
        <div class="button-group">
            <a href="@Url.Action("GetAllDrinks", "DrinkGui")" class="upper-btn">Список напитков</a>
            <a href="@Url.Action("GetAllCompanies", "CompanyGui")" class="upper-btn">Список сетей кофеен</a>
            @* @if (Model.Id_role == 3)
            {
                <a href="@Url.Action("GetAllUsers", "User")" class="upper-btn">Список пользователей</a>
            } *@
            @* <a href="@Url.Action("GetAllCompanies", "Company")" class="back-btn">Назад</a> *@
        </div>
    </div>
    <div class="coffeeshops-container">
        <div class="header-with-button">
            <h2>Список кофеен:</h2>
            @* @if (Model.Id_role == 2 || Model.Id_role == 3)
            {
                <a href="@Url.Action("AddCoffeeShopByCompany", "CoffeeShop", new { id_company = Model.Id_company })"
                   class="add-coffeeshop-btn">+ Добавить новую кофейню</a>
            } *@
        </div>

        @if (Model.CoffeeShops.Any())
        {
            <table class="coffeeshops-table">
                <thead>
                    <tr>
                        <th class="address-column">Адрес</th>
                        <th class="hours-column">Часы работы</th>
                        @* <th class="actions-column">Действия</th> *@
                    </tr>
                </thead>
                <tbody>
                    @foreach (var shop in Model.CoffeeShops)
                    {
                        <tr>
                            <td class="address-column">@shop.Address</td>
                            <td class="hours-column">@shop.WorkingHours</td>
                            @* <td class="actions-column">
                                <div class="action-buttons">
                                    <a href="@Url.Action("AddToFavs", "CoffeeShop", new { coffeeshop_id = shop.Id_coffeeshop , page = shop.Id_company})"
                                       class="action-btn">Добавить в избранные</a>
                                    @if (Model.Id_role == 2 || Model.Id_role == 3)
                                    {
                                        <a href="@Url.Action("DeleteCoffeeShop", "CoffeeShop", new { id_cs = shop.Id_coffeeshop, id_comp = shop.Id_company })"
                                           class="action-btn">Удалить</a>
                                    }
                                </div>
                            </td> *@
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p class="empty-message">У сети нет открытых кофеен</p>
        }
    </div>
</body>
</html>