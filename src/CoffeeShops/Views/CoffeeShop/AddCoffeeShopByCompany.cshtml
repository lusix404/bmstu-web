@* @model CoffeeShops.ViewModels.AddCoffeeShopViewModel

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="~/css/log_reg_style.css" />
    <link rel="stylesheet" href="~/css/errorstyle.css" />
    <link rel="stylesheet" href="~/css/header_container_style.css" />
    <link rel="stylesheet" href="~/css/logout_button.css" />
    <title>Registration</title>
    <style>
        .upper-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #5E463D;
            font-size: 18px;
            background-color: #EDE7DD;
            margin-right: 10px;
        }

        .back-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #EDE7DD;
            font-size: 18px;
            background-color: #2c1c1a;
        }

        .button-group {
            display: flex;
            position: fixed;
            top: 20px;
            left: 20px;
            z-index: 100;
        }

        main {
            background-color: #d3b997;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 400px;
            margin-top: 80px; /* Добавляем отступ сверху для кнопок */
        }

        .back-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #EDE7DD;
            font-size: 18px;
            background-color: #2c1c1a;
        }
    </style>
</head>

<body>
    <!-- Кнопки вынесены за пределы main -->
    <div class="button-group">
        <a href="@Url.Action("Logout", "User")" class="logout-btn">Выйти</a>
        <a href="@Url.Action("GetCoffeeShopsByCompany", "CoffeeShop", new { companyId = Model.Id_company })"
           class="back-btn">Назад</a>
    </div>

    <!-- Toast для ошибок -->
    <div id="toastContainer" style="position: fixed; bottom: 20px; right: 20px; z-index: 1000;">
        <div id="errorToast" class="toast" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="toast-header">
                <strong class="me-auto">Error</strong>
                <button type="button" class="btn-close" data-dismiss="toast" aria-label="Close"></button>
            </div>
            <div class="toast-body" id="toastErrorBody"></div>
        </div>
    </div>

    <main>
        <form method="post">
            @Html.AntiForgeryToken()
            <h3>Добавление новой кофейни:</h3>

            <div class="form-group">
                <label asp-for="Address"></label>
                <div class="col-md-5">
                    <input asp-for="Address" name="Address" class="form-control" placeholder="Адрес" />
                    <span asp-validation-for="Address" class="text-danger"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="WorkingHours"></label>
                <div class="col-md-5">
                    <input asp-for="WorkingHours" name="WorkingHours" class="form-control" placeholder="Часы работы" />
                    <span asp-validation-for="WorkingHours" class="text-danger"></span>
                </div>
            </div>

            <div class="form-group text-center">
                <input type="submit" value="Добавить" class="btn btn-primary" />
            </div>
        </form>
    </main>

    <!-- Минимальный JavaScript для работы Toast -->
    <script>
        @if (!ViewData.ModelState.IsValid)
        {
            <text>
                document.addEventListener('DOMContentLoaded', function () {
                    const errors = [];
                @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                {
                    @:errors.push('@Html.Raw(error.ErrorMessage.Replace("'", "\\'"))');
                }

                                        if (errors.length > 0) {
                        const toastBody = document.getElementById('toastErrorBody');
                        toastBody.innerHTML = `<ul style="margin:0;padding-left:20px;">${errors.map(e => `<li>${e}</li>`).join('')}</ul>`;

                        const toastEl = document.getElementById('errorToast');
                        toastEl.classList.add('show');

                        // Автоматическое скрытие через 5 секунд
                        setTimeout(() => {
                            toastEl.classList.remove('show');
                        }, 5000);

                        // Закрытие по кнопке
                        document.querySelector('[data-dismiss="toast"]').addEventListener('click', () => {
                            toastEl.classList.remove('show');
                        });
                    }
                });
            </text>
        }
    </script>
</body>
</html> *@


                        @model CoffeeShops.ViewModels.AddCoffeeShopViewModel

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="~/css/log_reg_style.css" />
    <link rel="stylesheet" href="~/css/errorstyle.css" />
    <link rel="stylesheet" href="~/css/header_container_style.css" />
    <link rel="stylesheet" href="~/css/logout_button.css" />
    <title>Добавление кофейни</title>
    <style>
        .back-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #EDE7DD;
            font-size: 18px;
            background-color: #2c1c1a;
        }

        .upper-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #5E463D;
            font-size: 18px;
            background-color: #EDE7DD;
        }

        .fav-btn {
            padding: 5px 10px;
            border-radius: 4px;
            text-decoration: none;
            color: #EDE7DD;
            font-size: 18px;
            background-color: #b567b2;
        }

        .button-group {
            display: flex;
            position: fixed;
            top: 20px;
            left: 20px;
            z-index: 100;
        }

        main {
            background-color: #d3b997;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 400px;
            margin-top: 80px;
        }

        .form-table {
            width: 100%;
            border-collapse: collapse;
        }

            .form-table td {
                padding: 8px 0;
                vertical-align: top;
            }

        .label-cell {
            width: 40%;
            padding-right: 10px;
        }

        .input-cell {
            width: 60%;
        }

        .submit-cell {
            text-align: center;
            padding-top: 15px;
        }

        .form-control {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
        }

        .text-danger {
            color: red;
            font-size: 0.875em;
        }
        /* Добавляем новые стили для ошибок */
        .error-container {
            width: 95%;
            margin: 0 auto 20px;
            padding: 15px;
            background-color: rgba(255, 107, 107, 0.1);
            border-left: 4px solid red;
            color: red;
            border-radius: 4px;
        }

        .error-message {
            margin: 0px 0;
            font-size: 14px;
        }
        /* Обновленные стили для блока ошибок */
        .form-errors {
            width: 100%;
            margin: 0 0 20px 0;
            padding: 15px;
            background-color: rgba(255, 107, 107, 0.1);
            border-left: 4px solid red;
            color: red;
            border-radius: 4px;
        }

        .error-item {
            margin: 0px 0;
            font-size: 14px;
        }
    </style>
</head>

<body>
    <div class="button-group">
        <a href="@Url.Action("Logout", "User")" class="back-btn">Выйти</a>
        <a href="@Url.Action("GetAllDrinks", "Drink")" class="upper-btn">Список напитков</a>
        <a href="@Url.Action("GetAllCompanies", "Company")" class="upper-btn">Список сетей кофеен</a>
        <a href="@Url.Action("GetFavDrinks", "Drink")" class="fav-btn">Список избранных напитков</a>
        <a href="@Url.Action("GetFavCoffeeShops", "CoffeeShop")" class="fav-btn">Список избранных кофеен</a>
        @if (Model.Id_role == 3)
        {
            <a href="@Url.Action("GetAllUsers", "User")" class="upper-btn">Список пользователей</a>
        }
        @* <a href="@Url.Action("GetCoffeeShopsByCompany", "CoffeeShop", new { companyId = Model.Id_company })"
           class="back-btn">Назад</a> *@
    </div>

   @*  @if (!ViewData.ModelState.IsValid)
    {
        <div class="error-container">
            @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
            {
                <div class="error-message">@error.ErrorMessage</div>
            }
        </div>
    } *@

    <main>
        <form method="post">
            @Html.AntiForgeryToken()
            <h3>Добавление новой кофейни:</h3>
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="form-errors">
                    @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                    {
                        <div class="error-item">@error.ErrorMessage</div>
                    }
                </div>
            }
            <table class="form-table">
                <tr>
                    <td class="label-cell">
                        <label asp-for="Address"></label>
                    </td>
                    <td class="input-cell">
                        <input asp-for="Address" name="Address" class="form-control" placeholder="Адрес" />
                        <span asp-validation-for="Address" class="text-danger"></span>
                    </td>
                </tr>
                <tr>
                    <td class="label-cell">
                        <label asp-for="WorkingHours"></label>
                    </td>
                    <td class="input-cell">
                        <input asp-for="WorkingHours" name="WorkingHours" class="form-control" placeholder="Часы работы" />
                        <span asp-validation-for="WorkingHours" class="text-danger"></span>
                    </td>
                </tr>
                <tr>
                    <td colspan="2" class="submit-cell">
                        <input type="submit" value="Добавить" class="btn btn-primary" />
                    </td>
                </tr>
            </table>
        </form>
    </main>

    
</body>
</html>